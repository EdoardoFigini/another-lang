N := 9U;

fib: (n : u32) -> u32 {
  if n < 2U {
    return n;
  } else {
    return fib(n - 1U) + fib(n - 2U);
  }
}

export main: () -> none {
  res := fib(N);
  std::io::println(res.to_str());
}

@import std;
